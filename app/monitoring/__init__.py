"""Spark Playbook — realtime cluster monitoring dashboard (Phase 2.5).

See docs/architecture/realtime-monitoring-dashboard.md for the ADR. Shares no
code with `app/annotation/` (separate tool, separate trigger model — see the
requirements doc's "Relationship to the Phase 2 annotation engine" section).
"""
