# Spark Playbook — test/dev dependencies (Phase 1 test suite).
#
# Install with:
#   pip install -r app/requirements.txt -r app/requirements-dev.txt
#
# Convention: runtime deps stay in app/requirements.txt; anything only needed
# to run the test suite (pytest + async support + FastAPI TestClient's HTTP
# transport) lives here.
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
