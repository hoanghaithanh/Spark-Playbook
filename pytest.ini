[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    integration: real end-to-end tests that spawn an actual Docker cluster (slow, requires Docker Desktop). Skipped by default; run with `pytest -m integration` or set RUN_DOCKER_TESTS=1.
